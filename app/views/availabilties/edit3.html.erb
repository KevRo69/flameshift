<div class="availabilities d-flex align-items-center">
  <%= simple_form_for [@user, @availability] do |f| %>
      <%= f.input :day,
                as: :string,
                label: "Disponibilités pour #{@month_next3}",
                input_html: { data: { controller: "datepicker-show3", datepicker_show3_availabilities_value: @availabilities_next_days3 } } %>
    <%= f.submit "Valider", class: "btn" %>
  <% end %>
</div>
